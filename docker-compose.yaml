version: "2"
services:

  homeassistant:
    build: ./homeassistant
    ports:
      - "8123"

  nginx:
    build: ./nginx
    depends_on:
      - homeassistant
    ports:
      - "80"

volumes:
  homeassistant:
