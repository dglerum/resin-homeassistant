version: "2"

volumes:
  homeassistant-data:

services:
  homeassistant:
    build: ./homeassistant
    ports:
      - "22"
      - "8123"
      - "137"
      - "138"
      - "139"
      - "445"
    network_mode: host
    volumes:
      - 'homeassistant-data:/data'
